# Remaining Debt or Remnants

- **Dead code only**: None found.
- **Reachable but hidden**: None found.
- **Reachable and must fix**: None found.
- **Naming debt only**: `isDebugBuild` variable is defined inside `SettingsScreen` and `QuickEntryScreen` functions in `MainActivity.kt` but remains unused, producing a Kotlin compiler warning. This is purely visual compiler output and has zero impact on functionality.
